# Environment Configuration for DemaNew

# Node Environment
NODE_ENV=development

# Server Configuration
PORT=3000
HOST=localhost

# Database Configuration (if applicable)
# DATABASE_URL=postgresql://user:password@localhost:5432/dbname
# MONGODB_URI=mongodb://localhost:27017/dbname

# API Keys (if applicable)
# API_KEY=your_api_key_here
# API_SECRET=your_api_secret_here

# External Services (if applicable)
# STRIPE_SECRET_KEY=sk_test_...
# SENDGRID_API_KEY=SG....

# Authentication (if applicable)
# JWT_SECRET=your_jwt_secret_here
# SESSION_SECRET=your_session_secret_here

# NextAuth
# NEXTAUTH_SECRET=generate_with_openssl_rand_base64_32
# NEXTAUTH_URL=http://localhost:3000
# GOOGLE_CLIENT_ID=...        # enables Google sign-in when both are set
# GOOGLE_CLIENT_SECRET=...

# Credentials login (email/password) is DISABLED unless configured here.
# Set both to enable a single known account; leave unset to force Google OAuth only.
# CREDENTIALS_LOGIN_EMAIL=admin@demashop.be
# CREDENTIALS_LOGIN_PASSWORD=use_a_strong_unique_password
# Dev-only convenience bypass (any non-empty password). NEVER honored in production.
# ALLOW_DEV_CREDENTIALS=false
# Comma-separated admin allowlist; these emails get the 'admin' role. (@demashop.be also admin via verified Google.)
# ADMIN_EMAILS=admin@demashop.be

# Logging
LOG_LEVEL=info

# Testing
# TEST_DATABASE_URL=postgresql://user:password@localhost:5432/test_db

# Feature Flags
# ENABLE_FEATURE_X=false

# IMPORTANT: 
# 1. Copy this file to .env
# 2. Replace placeholder values with actual values
# 3. Never commit .env to version control
# 4. Keep .env.example up to date with new variables
